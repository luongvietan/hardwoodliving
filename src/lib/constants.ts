/**
 * Shared application constants.
 * Single source of truth for values used across multiple files.
 */

/** Base URL for the site, used in sitemap, canonical URLs, structured data, etc. */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL || "https://hardwoodliving.net";
